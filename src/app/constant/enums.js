const roles = {
  SUPERADMIN: "superAdmin",
  ADMIN: "admin",
};

const queryFields = {
  SEARCH_TERM: "searchTerm",
  FILTER: "filter",
  PAGE: "page",
  LIMIT: "limit",
  SORT_BY: "sortBy",
  SORT_ORDER: "sortOrder",
  TIME: "time",
};

const ROLES = ["superAdmin", "admin"];

const studentSearchType = {
  ROLL: "roll",
  PHONE: "phone",
  QR: "qr",
};

export const Enums = {
  roles,
  queryFields,
  ROLES,
  studentSearchType,
};
